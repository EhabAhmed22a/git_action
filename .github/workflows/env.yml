name: Deploy to Staging
on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    # This is the key that triggers everything below
    environment: staging
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Simulate deployment
        run: |
          echo "Deploying $GITHUB_SHA to staging..."
          # Your actual deployment scripts (e.g., to Heroku, Kubernetes, AWS) would go here