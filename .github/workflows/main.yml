name: test push filters
on:
  push:
    branches: [main]
    paths:
      - 'src/**'
concurrency: 
  group: one_at_a_time
  cancel-in-progress: false
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "this is the push"
